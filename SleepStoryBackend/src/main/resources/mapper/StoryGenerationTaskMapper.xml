<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sleepstory.backend.dal.mapper.StoryGenerationTaskMapper">

    <resultMap id="BaseResultMap" type="com.sleepstory.backend.dal.po.StoryGenerationTaskPO">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="keywords" column="keywords"/>
        <result property="scene" column="scene"/>
        <result property="mood" column="mood"/>
        <result property="duration" column="duration"/>
        <result property="status" column="status"/>
        <result property="storyId" column="story_id"/>
        <result property="errorMessage" column="error_message"/>
        <result property="progressPercent" column="progress_percent"/>
        <result property="statusMessage" column="status_message"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="completedAt" column="completed_at"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, keywords, scene, mood, duration, status, story_id, error_message,
        progress_percent, status_message, created_at, updated_at, completed_at
    </sql>

    <insert id="insert" parameterType="com.sleepstory.backend.dal.po.StoryGenerationTaskPO">
        INSERT INTO story_generation_tasks (id, user_id, keywords, scene, mood, duration,
                                            status, story_id, error_message, progress_percent,
                                            status_message, created_at, updated_at, completed_at)
        VALUES (#{id}, #{userId}, #{keywords}, #{scene}, #{mood}, #{duration},
                #{status}, #{storyId}, #{errorMessage}, #{progressPercent},
                #{statusMessage}, #{createdAt}, #{updatedAt}, #{completedAt})
    </insert>

    <update id="update" parameterType="com.sleepstory.backend.dal.po.StoryGenerationTaskPO">
        UPDATE story_generation_tasks
        SET user_id = #{userId},
            keywords = #{keywords},
            scene = #{scene},
            mood = #{mood},
            duration = #{duration},
            status = #{status},
            story_id = #{storyId},
            error_message = #{errorMessage},
            progress_percent = #{progressPercent},
            status_message = #{statusMessage},
            updated_at = #{updatedAt},
            completed_at = #{completedAt}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM story_generation_tasks
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM story_generation_tasks
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM story_generation_tasks
        WHERE status = #{status}
        ORDER BY created_at ASC
    </select>

    <select id="selectLatestByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM story_generation_tasks
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM story_generation_tasks WHERE status = #{status}
    </select>

    <delete id="deleteById">
        DELETE FROM story_generation_tasks WHERE id = #{id}
    </delete>

    <delete id="deleteOldTasks">
        DELETE FROM story_generation_tasks
        WHERE created_at &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </delete>

</mapper>
